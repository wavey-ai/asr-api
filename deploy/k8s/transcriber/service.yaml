apiVersion: v1
kind: Service
metadata:
  name: transcriber
  namespace: transcriber
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: transcriber
  ports:
    - name: https
      port: 8443
      targetPort: https
