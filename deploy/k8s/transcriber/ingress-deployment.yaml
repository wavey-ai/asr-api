apiVersion: apps/v1
kind: Deployment
metadata:
  name: transcriber-ingress
  namespace: transcriber
  labels:
    app.kubernetes.io/name: transcriber-ingress
    app.kubernetes.io/component: ingress
    app.kubernetes.io/part-of: transcriber
spec:
  replicas: 2
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: transcriber-ingress
  template:
    metadata:
      labels:
        app.kubernetes.io/name: transcriber-ingress
        app.kubernetes.io/component: ingress
        app.kubernetes.io/part-of: transcriber
    spec:
      serviceAccountName: transcriber
      terminationGracePeriodSeconds: 120
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: node.kubernetes.io/instance-type
                    operator: NotIn
                    values:
                      - g2-gpu-rtx4000a1-s
      containers:
        - name: ingress
          image: ghcr.io/wavey-ai/transcriber:main
          imagePullPolicy: IfNotPresent
          ports:
            - name: https
              containerPort: 8443
          envFrom:
            - configMapRef:
                name: transcriber-ingress-config
            - secretRef:
                name: transcriber-env
                optional: true
          env:
            - name: TLS_CERT_PATH
              value: /var/run/transcriber/tls/tls.crt
            - name: TLS_KEY_PATH
              value: /var/run/transcriber/tls/tls.key
          volumeMounts:
            - name: tls
              mountPath: /var/run/transcriber/tls
              readOnly: true
          startupProbe:
            httpGet:
              scheme: HTTPS
              path: /healthz
              port: https
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 60
          readinessProbe:
            httpGet:
              scheme: HTTPS
              path: /healthz
              port: https
            periodSeconds: 5
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              scheme: HTTPS
              path: /healthz
              port: https
            initialDelaySeconds: 30
            periodSeconds: 15
            timeoutSeconds: 5
          resources:
            requests:
              cpu: "1"
              memory: 2Gi
            limits:
              cpu: "4"
              memory: 6Gi
      volumes:
        - name: tls
          secret:
            secretName: transcriber-tls
