apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: asr-api-model
  namespace: asr-api
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: linode-block-storage-retain
  resources:
    requests:
      storage: 32Gi
